apiVersion: solution.sap/v1
kind: Solution

metadata:
  name: btp-account-usage-b23dc
  version: 1.0.0
  description: >
    AI agent that monitors SAP BTP account usage, resource consumption,
    costs, and entitlements across subaccounts — powered by Joule (GPT-4o).

assets:
  - ref: ./assets/btp-usage-agent/asset.yaml
